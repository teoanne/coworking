<% all_spaces = link_to("Worldwide", spaces_path) %>
<%= render 'shared/title', title: "#{all_spaces} &raquo; Coworking spaces in #{@category.id}".html_safe %>

<% @category.spaces.each do |space| %>
  <%= render 'spaces/space', space: space %>
<% end %>